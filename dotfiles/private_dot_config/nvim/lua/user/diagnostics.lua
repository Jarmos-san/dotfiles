-- Custom configurations for showcasing the diagnostics of the current buffer.
-- See ":h vim.diagnostics.config()" for more info on the same topic.
return {
  virtual_text = true,
  underline = true,
  update_in_insert = false,
}
